# 🚀 Быстрый старт - BookStore API

## 🎯 Что это такое?

BookStore API - это современная, production-ready система управления книгами, построенная с использованием лучших практик Python разработки и DevOps.

### ⚡ Ключевые возможности

- **FastAPI** - современный, быстрый веб-фреймворк
- **JWT аутентификация** - безопасная система авторизации
- **SQLAlchemy ORM** - работа с базой данных
- **Comprehensive тестирование** - 95%+ покрытие кода
- **Docker контейнеризация** - легкое развертывание
- **Kubernetes поддержка** - масштабируемость
- **Мониторинг** - Prometheus + Grafana
- **CI/CD пайплайн** - автоматизация

## 🏃‍♂️ Быстрый запуск (3 минуты)

### Вариант 1: Один скрипт (Рекомендуется)

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd bookstore-api

# Запустите скрипт настройки
./scripts/setup-dev.sh

# Запустите сервер разработки
make dev
```

### Вариант 2: Docker (Самый простой)

```bash
# Запустите все сервисы с Docker
make docker-dev

# API доступно по адресу: http://localhost:8000
# Документация: http://localhost:8000/docs
```

### Вариант 3: Ручная настройка

```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
pip install -r fastapi_requirements.txt

# Настройте окружение
cp .env.example .env

# Запустите тесты
pytest

# Запустите сервер
python run_bookstore.py
```

## 📖 Первые шаги

### 1. Откройте документацию API

Перейдите по адресу: http://localhost:8000/docs

Здесь вы найдете:
- ✅ Интерактивную документацию всех эндпоинтов
- ✅ Возможность тестировать API прямо в браузере
- ✅ Схемы данных и примеры запросов

### 2. Создайте первого пользователя

```bash
# POST /api/v1/users/
curl -X POST "http://localhost:8000/api/v1/users/" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "username": "admin",
    "password": "admin123",
    "full_name": "Администратор",
    "is_superuser": true
  }'
```

### 3. Войдите в систему

```bash
# POST /auth/login
curl -X POST "http://localhost:8000/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin@example.com&password=admin123"
```

Сохраните полученный `access_token` для дальнейших запросов.

### 4. Создайте первую книгу

```bash
# POST /api/v1/books/
curl -X POST "http://localhost:8000/api/v1/books/" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -d '{
    "title": "Война и мир",
    "description": "Роман-эпопея Льва Толстого",
    "price": 599.99,
    "isbn": "9785170123456"
  }'
```

## 🔧 Основные команды

### Разработка

```bash
make dev              # Запустить сервер разработки
make test             # Запустить все тесты
make test-coverage    # Тесты с покрытием кода
make lint             # Проверить код линтерами
make format           # Отформатировать код
```

### Docker

```bash
make docker-build     # Собрать Docker образ
make docker-dev       # Запустить среду разработки
make docker-prod      # Запустить production среду
make logs             # Показать логи приложения
```

### Тестирование

```bash
make test-unit        # Unit тесты
make test-integration # Интеграционные тесты
make test-property    # Property-based тесты
make test-performance # Тесты производительности
```

### Безопасность

```bash
make security-scan    # Сканирование безопасности
make health           # Проверить здоровье приложения
```

## 📊 Структура API

### 🔐 Аутентификация

```
POST /auth/login      # Вход в систему
GET  /auth/me         # Информация о текущем пользователе
```

### 📚 Управление книгами

```
GET    /api/v1/books/           # Список книг (поиск, фильтры)
POST   /api/v1/books/           # Создать книгу (админ)
GET    /api/v1/books/{id}       # Детали книги
PUT    /api/v1/books/{id}       # Обновить книгу (админ)
DELETE /api/v1/books/{id}       # Удалить книгу (админ)
GET    /api/v1/books/stats      # Статистика по книгам
```

### 👥 Пользователи

```
GET    /api/v1/users/           # Список пользователей (админ)
POST   /api/v1/users/           # Регистрация
GET    /api/v1/users/{id}       # Профиль пользователя
PUT    /api/v1/users/{id}       # Обновить профиль
DELETE /api/v1/users/{id}       # Удалить пользователя (админ)
```

### ⭐ Отзывы

```
GET    /api/v1/reviews/         # Список отзывов
POST   /api/v1/reviews/         # Создать отзыв
PUT    /api/v1/reviews/{id}     # Обновить отзыв
DELETE /api/v1/reviews/{id}     # Удалить отзыв
```

### 📖 Списки для чтения

```
GET    /api/v1/reading-lists/           # Мои списки
GET    /api/v1/reading-lists/public     # Публичные списки
POST   /api/v1/reading-lists/           # Создать список
POST   /api/v1/reading-lists/{id}/books # Добавить книгу
DELETE /api/v1/reading-lists/{id}/books/{book_id} # Удалить книгу
```

### 🏥 Системные эндпоинты

```
GET /health           # Проверка здоровья
GET /metrics          # Метрики Prometheus
GET /info             # Информация о приложении
```

## 🎓 Обучающие материалы

### Документация

- **[Структура проекта](docs/СТРУКТУРА_ПРОЕКТА.md)** - Подробный обзор архитектуры
- **[Руководство по тестированию](docs/РУКОВОДСТВО_ПО_ТЕСТИРОВАНИЮ.md)** - Все о тестировании
- **[Docker и DevOps](docs/DOCKER_И_DEVOPS_РУКОВОДСТВО.md)** - Развертывание и мониторинг

### Примеры кода

- **[FastAPI примеры](examples/fastapi_cheatsheet.md)** - Изучение FastAPI
- **[Тестирование примеры](examples/testing_cheatsheet.md)** - Лучшие практики тестирования
- **[ООП примеры](examples/oop_cheatsheet.md)** - Объектно-ориентированное программирование

## 🚀 Варианты развертывания

### Локальная разработка

```bash
make dev
# Доступно по адресу: http://localhost:8000
```

### Docker разработка

```bash
make docker-dev
# Полный стек с базой данных и кэшем
```

### Production развертывание

```bash
make deploy-prod
# SSL, мониторинг, резервное копирование
```

### Kubernetes

```bash
make k8s-deploy
# Автомасштабирование, высокая доступность
```

## 🔍 Мониторинг и отладка

### Логи приложения

```bash
make logs              # Docker логи
tail -f logs/app.log   # Файловые логи
```

### Метрики

```bash
curl http://localhost:8000/metrics  # Prometheus метрики
make health                         # Проверка здоровья
```

### Дашборды

- **Grafana**: http://localhost:3000 (admin/admin)
- **Prometheus**: http://localhost:9090

## ❓ Часто задаваемые вопросы

### Q: Как изменить порт приложения?

A: Отредактируйте переменную `PORT` в файле `.env` или `run_bookstore.py`

### Q: Как добавить новый эндпоинт?

A: 
1. Создайте новый роутер в `bookstore/routers/`
2. Добавьте его в `bookstore/main.py`
3. Напишите тесты в `tests/`

### Q: Как настроить базу данных?

A: Отредактируйте `DATABASE_URL` в файле `.env`

### Q: Как запустить только тесты?

A: `make test` или `pytest tests/`

### Q: Как развернуть в production?

A: Следуйте инструкциям в `docs/PRODUCTION_DEPLOYMENT.md`

## 🆘 Получение помощи

### Документация

- Проверьте папку `docs/` для подробных руководств
- Изучите примеры в папке `examples/`
- Прочитайте комментарии в коде

### Отладка

```bash
# Проверьте логи
make logs

# Проверьте здоровье
make health

# Запустите диагностику
./scripts/production-health-check.sh
```

### Сообщество

- Создайте Issue на GitHub
- Проверьте существующие Issues
- Прочитайте CONTRIBUTING.md

## 🎉 Поздравляем!

Теперь у вас есть полностью функциональный, production-ready API! 

**Следующие шаги:**
1. Изучите документацию API по адресу `/docs`
2. Запустите тесты: `make test`
3. Попробуйте создать книги и пользователей
4. Изучите мониторинг и логи
5. Разверните в production

**Удачного программирования! 🚀**